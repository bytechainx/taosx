# taosx 上下文

本文件定义 `taosx` 与其使用方共享的核心词汇。它只记录领域含义与能力边界，
不记录具体实现、API 签名、存储或部署决定。

## 传输与连接

**REST 传输**：默认数据通道，即 `POST /rest/sql[/database]`（默认端口 6041，Basic 认证）；
它是读写主路径，不是「兜底方案」。
_Avoid_: HTTP 接口（过于笼统，无法与 WS 通道区分）

**原生 WebSocket 传输**（`TransportMode::NativeWs`）：同端口 `/rest/ws` 上的补充通道，
适合频繁小请求；它**不**自动接管 SQL 数据面——除非显式调用 `exec_sql_ws`，
读写仍走 REST。
_Avoid_: 长连接（本仓库的 WS 用法是短会话，不是常驻订阅）

**握手探测**（`connect_native_ws`）：带 deadline 的 WS 建连尝试，成功即关闭；它只证明
通道可达，不代表任何 SQL 已执行。
_Avoid_: 连接建立（探测结束后并不会保留这个连接）

**故障转移**（`hosts`）：`host` 失败后按序尝试备用主机的行为；它是同一配置下的主机列表，
不是跨集群路由或负载均衡。
_Avoid_: 高可用（备用主机只做顺序重试，没有健康打分或流量分配）

**端口探测**（`probe_native_tcp`）：对调用方指定的原生 SQL 端口（TDengine 惯用 `6030`）做
可达性检查，**不**发送任何协议帧；它不能替代 WS 握手探测或 `ping`。
_Avoid_: 连通性检查（这个函数只回答「端口开着吗」）

## 写入与标识符

**数据点**（`TaosPoint`）：一次写入的最小单元，固定为 tag 值 + 纳秒时间戳 + 两个数据列；
它不表达任意列数的行，也不是 schema 描述。
_Avoid_: 行记录（本仓库的数据点结构是固定的，不能泛化为任意表结构）

**超级表**（STABLE）：写入目标的表名，必须通过标识符白名单；它只被当作名字使用，
本仓库不管理其 schema。
_Avoid_: 表模型（本仓库不建模列类型与 tag 定义）

**子表**：由 `{stable}_s{tag 值 UTF-8 十六进制}` 生成的写入落点；tag 值不直接进入标识符，
因此反引号、引号与路径字符天然无法逃逸。
_Avoid_: 分区表（子表是按 tag 派生出的表名，不是时间分区）

**标识符白名单**：库名与超级表名的准入规则（首字符为字母或下划线，其余仅 `[A-Za-z0-9_]`，
表名 ≤ 94 字节、标识符 ≤ 192 字节），非法即返回 `TaosError::Invalid`。
_Avoid_: 转义（标识符走拒绝而非转义，两者不能混用）

**批量分块**：按 `batch_max_rows` 与 `batch_max_bytes` 把一批数据点切成多条 INSERT；
分块是资源约束的产物，**不**承诺跨分块原子性。
_Avoid_: 事务批次（没有任何跨分块回滚语义）

## 精度与上限

**时间精度**（`TsPrecision`）：目标库的时间列精度，取值 `Ms` / `Us` / `Ns`；配置字段
`precision` 默认为 `None`，由连接后探测填入。
_Avoid_: 时间单位（精度是库的属性，不是本地格式化偏好）

**禁止静默精度损失**：纳秒时间戳无法无损换算为目标精度时直接报错的约定；它取代了主工程
外部的 `PrecisionLossPolicy`，由 `build_insert_sql_chunks` 内联执行。
_Avoid_: 截断（本仓库拒绝截断，不提供「按需降精度」的开关）

**硬上限**（`HARD_MAX_*`）：并发、批量行数 / 字节、响应体、查询行数与关闭 deadline 的构建期
上界，超出的配置值被 clamp 或拒绝。
_Avoid_: 默认值（硬上限是拒绝线，不是可调默认）

**有界查询流**（`TaosQueryStream`）：先按 `max_query_rows` 有界物化、再逐行 yield 的结果流；
它**不是**服务端游标，`chunk_hint` 只是拉取块大小提示。
_Avoid_: 游标（服务端游标可以在任意时刻回到下一批，本实现是先取满再消费）

## 可靠性与观测

**可重试错误**：`is_retryable()` 判定为真的一类失败，仅 `Connection` / `Unavailable` /
`Timeout` / `Io`；它与「配置、鉴权、语法等永久错误」互补。
_Avoid_: 服务端错误（`Backend` 明确不可重试，成因不能决定可重试性）

**重试策略**（`RetryPolicy`）：指数退避 + 抖动 + deadline 的组合，给出读与幂等写预设；
它不改变错误分类，只决定「再试几次、隔多久」。
_Avoid_: 重连（重试策略作用于单次操作，不重建整个连接池）

**指标快照**（`TaosMetricsSnapshot` / `ws_probe_totals`）：对池状态与 WS 探测计数的只读采样；
它不做上报，也不承诺采样时刻的一致性。
_Avoid_: 监控后端（本仓库只产出快照，导出由调用方负责）

**fail-closed**：遇到无法确证成功的信号（如 WS 空帧、精度不一致、越界参数）时一律报错而不是
猜测成功的约定。
_Avoid_: 容错（fail-closed 的下场是报错，不是降级继续）
